@model Connector

<div id="codeLoad">
    <tr>
        <td>Game Code:</td>
        <td>@Model.GameCode</td>
    </tr>
    @if (!string.IsNullOrEmpty(@Model.GameCode))
    {
        <button id="rejoinQueue" class="btn-primary">Rejoin Queue</button>
    }
</div>